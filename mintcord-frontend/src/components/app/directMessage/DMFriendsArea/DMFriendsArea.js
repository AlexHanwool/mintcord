import React from 'react';

import styles from './DMFriendsArea.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const DMFriendsArea = () => {
  return (
    <div>
      FriendSelector
      FriendList
    </div>
  );
};

export default DMFriendsArea;